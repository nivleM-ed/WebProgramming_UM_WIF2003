$(document).ready(function(){
    var arrsize = $(".route-main-pane .stay-row .marker").length;

    var arr = Array(arrsize);
    
    // console.log();

    // for( var i=0; i<arrsize; i++ ){
    //     console.log($(".route-rows .draggable").eq(i).attr("id"));
    //     console.log("eh " + (i+1));
    // }
    
      
});